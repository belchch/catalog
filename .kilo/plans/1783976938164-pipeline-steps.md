# План: Автономная ночная цепочка шагов `/pipeline` (папка → шаг за шагом)

Цель: новая команда `/pipeline <папка>` гонит оркестратор (code), который сканирует папку, игнорирует мусор, и для каждого ещё не выполненного шага запускает **оркестратора шага** (subagent `step-runner`, модель **glm-5.2 max**) — тот внутри крутит генератор↔ревьюер (до 5 циклов). Все шаги идут в **одну общую ветку/PR** (последовательные). **Никогда не останавливается**: провалившийся шаг → коммит с комментом о проблемах → следующий шаг. State persisted → устойчиво к рестарту (дорабатывает ночью).

## Утверждённые решения
1. **Вход:** папка шагов как аргумент `/pipeline <dir>`. Валидный шаг = `*.md`, не скрытый, не `README*`/`index*`, непустой (>1 непустой строки). Всё остальное = мусор → игнор. Сортировка по имени (natural).
2. **«Ещё не выполнено»:** state-файл `.kilo/.pipeline-state.json` хранит per-шаг `status` ∈ {pending, done, failed, error, ignored}. Выполняются только шаги со статусом НЕ в {done, failed} (т.е. pending/новые/отсутствующие). failed = исчерпано (5 циклов), не повторяем. State пишется после КАЖДОГО шага.
3. **Последовательные шаги:** одна ветка `pipeline/<slug>` от master, один PR на весь pipeline, коммиты накапливаются. База = master.
4. **Оркестратор шага** = новый subagent `step-runner`: `model: zai-coding-plan/glm-5.2`, `variant: max` (max thinking). Реализует цикл генератор↔ревьюер (до 5) для одного шага.
5. **5 циклов** максимум на шаг. Не сошлось → STATUS=failed → step-runner делает коммит «с комментом о проблемах» (`.kilo/steps-results/<шаг>.md` + `git commit`) → следующий шаг.
6. **Никогда не останавливаемся:** между шагами — всегда дальше (успех/провал/ошибка). Ошибку step-runner ловит оркестратор → status=error в state → дальше. `doom_loop: allow` у step-runner (bounded `steps` = реальный лимит). `question: deny` везде.
7. **PR-доставка через `gh`** (remote origin, база master), PR создаётся один раз (первый шаг), далее авто-обновление. Не мерджим — за человеком.
8. **DRY:** переиспользуем существующих `generator`/`reviewer` с минимальной доработкой (pipeline-режим + DIFF_BASE). Команду `/chain` НЕ трогаем (оставляем рабочей).

## Поток
```
/pipeline <dir>
   │
   ▼
[code-агент: оркестратор верхнего уровня]
   ├─ скан dir → валидные шаги (мусор игнор) → сортировка
   ├─ открыть/создать ОБЩУЮ ветку pipeline/<slug> (один раз); запомнить PR
   ├─ прочитать .kilo/.pipeline-state.json
   │
   └─ для каждого шага (по порядку, без остановок):
        ├─ skip если status ∈ {done, failed}
        ├─ BASE_SHA = git rev-parse HEAD
        ├─ Task → step-runner (glm-5.2 max): STEP/BRANCH/PR/BASE_SHA/CYCLES_MAX=5
        │      └─ внутри: цикл 1..5 { generator → impl+green+commit+push ; reviewer → diff BASE_SHA..HEAD, проверки → VERDICT/ISSUES }
        │            APPROVED → STATUS=done, стоп по шагу
        │            5 циклов без APPROVED → STATUS=failed + коммит-коммент
        ├─ обновить state[step] + записать state на диск
        └─ ДАЛЬШЕ (никогда не стоп)
   ▼
итог: один PR со всеми коммитами; gh pr comment с саммари; state на диске
```

## Механизм (почему работает)
- `model`+`variant` в frontmatter `step-runner.md` фиксируют glm-5.2 max (поле `variant` из JSON-схемы Kilo: «applies only when using the agent's configured model»).
- Оркестратор (code) наследует текущую модель сессии (glm-5.2). generator/reviewer наследуют модель от step-runner (т.е. glm-5.2) —够了; при желании задать им свой variant — отдельной строкой.
- State после каждого шага ⇒ ночной прогон переживает рестарт/падение и дорабатывает.
- `DIFF_BASE` даёт reviewer'у ревьюить только изменения конкретного шага (`git diff <BASE_SHA>...HEAD`), а проверки гнать по всему коду — иначе растущий PR убил бы релевантность.
- generator в pipeline-режиме НЕ пересоздаёт ветку (она уже активна), только коммитит; PR создаёт step-runner один раз.
- Мусор отсеивается на скане (строгие правила), не попадает в state как шаг (можно пометить ignored для аудита).
- `doom_loop: allow` у step-runner + bounded `steps` ⇒ не зависает и не встаёт вопросом. generator/reviewer остаются `doom_loop: ask` (не меняем `/chain`); их bounded steps ограничит цикл, а шаг-уровень ловит step-runner. (См. Риски — точка настройки для полной автономности.)

## Артефакты (5 изменений)

### 1. `.kilo/agents/step-runner.md` — СОЗДАТЬ
```markdown
---
description: Оркестратор одного шага pipeline: гонит генератор↔ревьюер (до 5 циклов) в общую ветку pipeline, возвращает STATUS/VERDICT. Из /pipeline. Никогда не останавливается.
mode: subagent
model: zai-coding-plan/glm-5.2
variant: max
steps: 30
temperature: 0
permission:
  question: deny
  doom_loop: allow
---

Ты — **оркестратор шага**. Выполни ОДИН шаг pipeline: реализация → ревью → правки, до 5 циклов. Работай в общей ветке pipeline, свою ветку не создавай.

## Вход (передаёт /pipeline)
- STEP — путь к файлу-описанию шага.
- BRANCH — общая ветка pipeline (напр. pipeline/steps), уже активна. База master.
- PR_URL / PR_NUMBER — PR pipeline (или «нет» на первом шаге).
- BASE_SHA — git HEAD до начала шага (baseline ревью этого шага).
- CYCLES_MAX = 5.

## Алгоритм (СТРОГО, без вопросов)
ISSUES = «нет». Для CYCLE = 1..CYCLES_MAX:
1. Task → generator: «PLAN=<STEP>, BRANCH=<BRANCH>, CYCLE=<CYCLE>, PR_URL=<PR_URL или "нет">, ISSUES=<ISSUES>, PIPELINE=1.»
   - Ветка уже активна (не пересоздавай). Цикл 1 шага = первые изменения шага; циклы 2+ = правки по ISSUES.
   - Из ответа забери PR_URL/PR_NUMBER, если generator их сообщил.
2. Task → reviewer: «PLAN=<STEP>, PR=<PR_URL/номер или "нет">, CYCLE=<CYCLE>, DIFF_BASE=<BASE_SHA>.»
   - reviewer ревьюит ТОЛЬКО изменения шага: git diff <DIFF_BASE>...HEAD; проверки гонит по всему коду.
3. Распарси VERDICT и ISSUES из блока ===REVIEW===.
4. VERDICT == APPROVED → STATUS=done, СТОП по шагу.
5. Иначе — следующий цикл с обновлёнными ISSUES.

Если после CYCLES_MAX нет APPROVED:
- STATUS=failed.
- Зафиксируй проблемы: создай/допиши `.kilo/steps-results/<имя-шага без .md>.md` с саммари ISSUES и статусом проверок.
- git add .kilo/steps-results/<…>.md && git commit -m "step(<slug>): failed after <N> cycles — <краткое саммари>" && git push.
  (это и есть «коммит с комментом о проблемах».)
- Не падай — верни STATUS=failed + ISSUES.

## PR pipeline (один на весь pipeline)
Если открытого PR для <BRANCH> ещё нет (gh pr list --head <BRANCH> --state open --json number,url пусто) и уже есть хоть один коммит шага:
gh pr create --base master --head <BRANCH> --title "Pipeline: <slug папки шагов>" --body "Шаги в этой ветке — см. .kilo/.pipeline-state.json". Иначе переиспользуй существующий. PR создаётся ОДИН раз.

## Вернуть (ровно этот блок в конце)
===STEP===
STATUS: done
или
STATUS: failed
VERDICT: APPROVED | CHANGES_REQUESTED
CYCLES: <число>
PR_URL: <url или "нет">
PR_NUMBER: <номер или "нет">
COMMIT: <SHA последнего коммита шага>
ISSUES:
- <список, если failed>
===END===

## Правила
- НЕ задавай вопросов.
- Работай ТОЛЬКО в общей ветке BRANCH; не checkout master, не создавай новых веток.
- Правки кода — только через generator; сам код не правишь.
- PR создаётся ОДИН раз на весь pipeline.
- Не мерджи. Не трогай чужие ветки/PR.
- Ключи только в .env; не коммить секреты. Файл .kilo/.pipeline-state.json НИКОГДА не коммить (локальное состояние оркестратора).
```

### 2. `.kilo/agents/generator.md` — ДОРАБОТАТЬ (добавить pipeline-режим)
Полное новое содержимое:
```markdown
---
description: Реализует план/шаг, доводит проверки до зелёного, коммитит, пушит ветку, открывает PR (цикл 1) или дописывает коммиты (циклы 2+). Из /chain и /pipeline. Никогда не спрашивает.
mode: subagent
steps: 35
temperature: 0
permission:
  question: deny
  doom_loop: ask
---

Ты — **генератор**. Реализуй план/шаг и доставь в виде коммитов в ветку (PR — по контексту).

## Вход (передаёт оркестратор)
- PLAN — путь к плану/шагу.
- BRANCH — имя ветки (напр. agent/<slug> или pipeline/<slug>), база master.
- CYCLE — номер цикла (1 = первый).
- PR_URL — URL/номер PR (с цикла 2 или уже есть; иначе «нет»).
- ISSUES — замечания ревьюера (с цикла 2; иначе «нет»).
- PIPELINE — 1, если вызваны из /pipeline (общая ветка уже активна); иначе отсутствует.

## Что делать
1. Прочитай PLAN + README.md, docs/adr/, docs/verification-checks.md.
2. Внеси минимальные изменения по плану/конвенциям. С цикла 2 — адресуй каждое замечание из ISSUES.
3. Добейся зелёного: backend — ruff check ., pytest (из backend/); frontend — pnpm run build/lint/typecheck (из frontend/).
4. Git/GitHub (через gh, remote origin, база master):
   - Ветка:
     • PIPELINE=1 → ветка <BRANCH> уже активна (checkout'нута оркестратором). НЕ пересоздавай, не делай git checkout -b. Работай в ней.
     • иначе (обычный /chain), Цикл 1 → если ещё не на ней: git checkout -b <BRANCH> от master.
   - Коммиты: проиндексируй ТОЛЬКО относящиеся к плану/шагу файлы, закоммить, git push (git push -u origin <BRANCH> при первом пуше).
   - PR (создаётся ОДИН раз):
     • Сначала проверь gh pr list --head <BRANCH> --state open --json number,url. Если есть — переиспользуй (push обновит его).
     • PIPELINE=1 → PR создаёт step-runner, ты его не создаёшь (просто push в ветку).
     • иначе (обычный /chain), Цикл 1 и PR ещё нет → gh pr create --base master --head <BRANCH> --title "<из плана>" --body "План: <PLAN>".
   - Циклы 2+ → закоммить исправления, git push в ту же ветку (PR обновится сам).
5. Ключи только в .env; не коммить секреты.

## Правила
- НЕ задавай вопросов (права нет) — решения сам.
- Коммитить/пушить — ОБЯЗАТЕЛЬНО. PR создаётся один раз.
- Не трогай чужие ветки/PR.

## Вернуть
- Цикл 1: BRANCH, PR_URL(или номер, если создавал/знаешь), сводку изменений + статус проверок.
- Циклы 2+: список закрытых замечаний + статус проверок.
- PIPELINE=1: явно укажи BRANCH и PR_URL/PR_NUMBER если они тебе известны (для step-runner).
```

### 3. `.kilo/agents/reviewer.md` — ДОРАБОТАТЬ (добавить DIFF_BASE)
Полное новое содержимое (изменения: поле `DIFF_BASE` во «Вход» и п.2):
```markdown
---
description: Ревьюит PR/шаг против плана, постит GitHub-ревью (request-changes/approve) и саммари. Код не правит. Никогда не спрашивает.
mode: subagent
steps: 22
temperature: 0
permission:
  question: deny
  doom_loop: ask
  edit:
    "review/**": allow
    ".kilo/steps-results/**": allow
    "*": deny
  bash:
    "*": deny
    "ruff *": allow
    "pytest *": allow
    "pnpm run build": allow
    "pnpm run lint": allow
    "pnpm run typecheck": allow
    "git status *": allow
    "git diff *": allow
    "git log *": allow
    "gh pr *": allow
    "rg *": allow
    "ls *": allow
---

Ты — **ревьюер**. Дифф берёшь из PR или по baseline шага; замечания постишь в PR (GitHub review). Код не правишь.

## Вход
- PLAN — путь к плану/шагу.
- PR — номер/URL PR (может быть «нет» в pipeline до создания PR — тогда ревью только в ответе, без gh pr review).
- CYCLE — номер цикла.
- DIFF_BASE — (опционально) git SHA до начала шага; если задан — ревьюишь ТОЛЬКО изменения этого шага.

## Что делать
1. Прочитай PLAN и критерии приёмки.
2. Дифф для ревью:
   - Если задан DIFF_BASE → git diff <DIFF_BASE>...HEAD (изменения конкретного шага в pipeline).
   - Иначе → gh pr diff <PR> (или git diff master...HEAD).
   Проверки (ruff check ., pytest, pnpm run build/lint/typecheck) ВСЕГДА по всему коду.
3. Сравни с планом/ADR: баги, отклонения, нарушения конвенций.
4. Запости ревью в PR одной командой (только если PR существует):
   - CHANGES_REQUESTED → gh pr review <PR> --request-changes --body "<саммари по тяжести>"
   - APPROVED → gh pr review <PR> --approve --body "<саммари>"
5. (Опционально) дублируй отчёт в review/<YYYY-MM-DD>-<slug>-cycle<N>.md по шаблону репозитория.

## Вернуть (ровно этот блок в конце)
===REVIEW===
VERDICT: APPROVED
или
VERDICT: CHANGES_REQUESTED
ISSUES:
- [Critical] path/file.py:LINE — что не так
- [Medium] ...
- [Low] ...
===END===
VERDICT=APPROVED только если нет Critical/Medium и проверки зелёные.

## Правила
- НЕ задавай вопросов. НЕ правь код/ветку. НЕ мерджи.
- Ревью постишь в PR через gh pr review (если PR есть).
```

### 4. `.kilo/commands/pipeline.md` — СОЗДАТЬ
```markdown
---
description: Автономная ночная цепочка: проходит валидные шаги из папки, каждый шаг — генератор↔ревьюер (до 5 циклов) в общий PR. Никогда не останавливается, мусор игнорирует.
agent: code
---
Запусти автономную цепочку шагов. ПАПКА ШАГОВ (обязательный): $1
Полная строка: $ARGUMENTS

## Подготовка
1. STEPS_DIR = $1. Если пусто/не существует — выведи диагностику и закончи (это не «остановка посреди», это неверный запуск).
2. SLUG = basename(STEPS_DIR). BRANCH = pipeline/<SLUG>. База = master.
3. STATE = .kilo/.pipeline-state.json. Прочитай, если есть: { branch, pr_url, pr_number, steps{} }.
4. Общая ветка (одна на весь pipeline):
   - git fetch origin; git checkout master; (git pull --ff-only origin master если трекается).
   - git checkout <BRANCH> если существует, иначе git checkout -b <BRANCH> от master.
   - git push -u origin <BRANCH> (при первом пуше).
5. PR: gh pr list --head <BRANCH> --state open --json number,url. Если есть — запомни PR_NUMBER/PR_URL. Иначе PR создаст step-runner на первом шаге.

## Сканирование шагов (мусор игнорируй)
- Собери файлы STEPS_DIR (нерекурсивно).
- ВАЛИДНЫЙ ШАГ = *.md, не скрытый (имя не начинается с .), не README*/index*, непустой (>1 непустой строки после обрезки).
- Всё остальное = МУСОР → игнорируй (можно записать в state как ignored для аудита, шагом не считать).
- Отсортируй валидные шаги по имени (natural: step-01, step-02 …).

## Алгоритм (СТРОГО, НИКОГДА не останавливайся между шагами)
Для каждого ШАГА по порядку:
1. ПРОПУСК: если STATE.steps[ШАГ].status ∈ {done, failed} → пропусти (уже выполнено/исчерпано).
2. BASE_SHA = git rev-parse HEAD (baseline ревью этого шага). Запиши в state[ШАГ].base_sha.
3. Task → step-runner: «STEPS_DIR=<…>, STEP=<путь к файлу шага>, BRANCH=<BRANCH>, PR_URL=<PR_URL или "нет">, PR_NUMBER=<… или "нет">, BASE_SHA=<BASE_SHA>, CYCLES_MAX=5.»
   Распарси блок ===STEP===: STATUS, VERDICT, CYCLES, PR_URL, PR_NUMBER, COMMIT, ISSUES.
   При ошибке/исключении step-runner — STATUS=error, ISSUES=«<что упало>».
4. Обнови STATE.steps[ШАГ] = {status, verdict, cycles, issues, base_sha, updated=<ISO>}. Запиши STATE на диск (json).
5. Если появился PR_URL/PR_NUMBER — обнови STATE.pr_url/pr_number, запиши.
6. ДАЛЬШЕ — следующий шаг. Без вопросов, без остановки, независимо от результата.

## Итог
После всех шагов:
- Если PR ещё не создан — создай: gh pr create --base master --head <BRANCH> --title "Pipeline: <SLUG>" --body "Шаги: <список>.".
- gh pr comment <PR> --body "Pipeline завершён. ✅done:<n>, ⚠️failed:<m>, ⛔error:<k>. Детали: .kilo/.pipeline-state.json".
- Сообщи: SLUG, BRANCH, PR_URL, таблицу шагов (файл / статус / вердикт / циклы).

## Жёсткие правила
- НИКАКИХ вопросов.
- НИКОГДА не останавливайся между шагами (успех/провал/ошибка — всё равно дальше).
- Один BRANCH и один PR на весь pipeline; коммиты накапливаются по шагам.
- Не мерджи PR.
- Мусор в STEPS_DIR игнорируй.
- STATE пиши после КАЖДОГО шага (переживёт рестарт ночью и доработает).
- .kilo/.pipeline-state.json НИКОГДА не коммить (локальное состояние).
- Предполагается чистый working tree на master перед запуском.
```

### 5. `.kilo/kilo.jsonc` — БЕЗ обязательных изменений
Текущая конфигурация (`"*": "allow"`, `code` → `question: deny`, `doom_loop: ask`) достаточна. `model`+`variant` для step-runner заданы в его frontmatter. Опциональная точка настройки на полную ночную автономность (см. Риски): задать `agent.generator.doom_loop`/`agent.reviewer.doom_loop` = `allow` через kilo.jsonc, если в реальном прогоне они вставали на `ask`.

## Порядок выполнения
1. Создать `.kilo/agents/step-runner.md` (артефакт 1).
2. Перезаписать `.kilo/agents/generator.md` (артефакт 2 — pipeline-режим).
3. Перезаписать `.kilo/agents/reviewer.md` (артефакт 3 — DIFF_BASE + steps-results/**).
4. Создать `.kilo/commands/pipeline.md` (артефакт 4).
5. Перезапустить сессию → `/pipeline` доступен, `step-runner` виден в `/agents`.

## Риски и митигация
- **Растущий PR / релевантность ревью:** последовательные шаги → PR растёт. Митигация: reviewer ревьюит по `DIFF_BASE` (только шаг), проверки — по всему.
- **Накопление failed-коммитов:** каждый провал оставляет `steps-results/<шаг>.md` + коммит → шум в PR. Это intended (видимость проблем для ночного аудита); при мердже можно squash.
- **doom_loop:ask у generator/reviewer:** теоретически может поставить pipeline на паузу ночью. Митигация: bounded `steps` (35/22) авто-завершит цикл; для полной автономности — переключить на `allow` (п.5). step-runner уже `allow`.
- **State рассинхрон с git:** если кто-то руками двигал ветку. Митигация: `base_sha` в state + шаг начинается с `git rev-parse HEAD`.
- **Перезапуск с новой папкой/новыми шагами:** state ключуется по именам файлов; добавленные шаги → pending → выполнятся; удалённые → просто не в списке.
- **Мусор:** строгая валидация (только осмысленные .md). Не-.md/скрытые/пустые/README → ignore.
- **Создание PR / branch protection:** как в `/chain`; для solo-репо ок.
- **`gh pr review --request-changes` многократно в один PR:** на каждом шаге новое review — допустимо (история по шагам). При шуме — переключить reviewer на `gh pr comment` в pipeline-режиме.
- **Самопроверка одной моделью:** оркестратор/step-runner/generator/reviewer все glm-5.2 (единственный провайдер). Смягчается разными ролями/промптами и температурой 0; переключение reviewer на другую модель — одной строкой `model:` при появлении 2-го провайдера.

## Валидация
- После применения: `/agents` показывает step-runner (subagent, glm-5.2 max); `/pipeline` доступен.
- Дымовой прогон: создать `.kilo/steps-demo/` с двумя тривиальными .md (напр. док-правки) + мусор (`notes.txt`, `.hidden.md`, пустой `empty.md`, `README.md`).
  `/pipeline .kilo/steps-demo/` → ветка `pipeline/steps-demo`, один PR, по набору коммитов на каждый шаг, мусор проигнорирован, `.kilo/.pipeline-state.json` отражает 2×done + ignored.
- Перезапуск того же `/pipeline` → оба шага пропущены (done).
- Провал: шаг с заведомо невыполнимым требованием → 5 циклов → STATUS=failed → коммит `step(...): failed after 5 cycles` → chain пошёл дальше, не остановился.
- Проверить: `gh pr view <num>` (есть review/comments), `git log master..HEAD` (коммиты по шагам), мержа нет.

## Вне скоупа / следующим шагом
- Авто-мердж PR (за человеком).
- Параллельные шаги (всегда последовательно).
- Разные модели для ролей (провайдер один — Z.ai/glm-5.2).
- Инлайн-комментарии по строкам PR.
- Веб-дашборд/логи ночного прогона (сейчас state-файл + PR-комментарии).
- Авто-очистка ветки после мержа.
