# CATALOG-23 — «Соединение закрыто» (idle / reconnect)

- **Задача Plane:** [CATALOG-23](https://app.plane.so/belchch/projects/catalog-app/work-items/23) (id: `0f4bbf4f-1360-45ba-9f80-ce8cf4fef5b5`, state: In Progress)
- **Статус плана:** Analyzed
- **Тип шага:** code
- **Цель:** Устранить стабильное появление «Соединение закрыто» ~каждые 5 минут (idle WS) и обеспечить серверный keepalive / адекватный timeout. UI-кнопка реконнект — в `CATALOG-23-ui-ws-reconnect.md`.

## Постановка задачи (актуальное ТЗ)
_(источник: последний комментарий от 2026-07-17)_

Задача не решена. Стабильно каждые примерно 5 минут появляется сообщение «Соединение закрыто». Похоже, нужно увеличить таймаут и сделать кнопку реконнект.

## Предыстория

> Это уже было: старое описание и предыдущие обсуждения. Приведено для контекста, НЕ является актуальным заданием.

**Описание:** Похоже пользователь наткнётся на это сообщение, если истекла сессия. Исправить только если проблема чётко определена. Если не понятно — просто закрыть.

**Комментарий 2026-07-16:** составлен plan investigate-first; если причина неясна — закрыть.

Дубликат/архив: `docs/plan/archive/2026-18-08_night-shift/CATALOG-23-connection-closed.md`.

## Контекст

- UI: `ws.onclose` → `closed` → «Соединение закрыто» — `usePlannerSession.ts:129-134`, `Chat.tsx:90`.
- Server WS: `await receive_text()` без ping — `sessions.py` (~241+); idle ~5 мин типичен для прокси/браузера.
- Ложный close от StrictMode cleanup (не сбрасывает `closed` при том же sessionId) — тоже кандидат, но актуальное ТЗ указывает на ~5 мин → idle timeout.
- Парный UI: `CATALOG-23-ui-ws-reconnect.md`.

## Затрагиваемые файлы

- `backend/app/api/sessions.py` — keepalive (ping/pong или периодический server frame), не рвать idle session.
- Возможно uvicorn/proxy настройки, если лимит снаружи.
- `backend/tests/` — при возможности тест на долгий idle / ping.

## План действий

1. Подтвердить: открыть чат, подождать ~5 мин без сообщений → воспроизвести close.
2. Добавить server-side keepalive на planner WS (и при необходимости run WS) с интервалом < 5 мин.
3. Убедиться, что нормальный turn/cancel не ломаются.
4. Не маскировать реальные `error`-кадры.

## Критерии приёмки (Definition of Done)

- [ ] Idle чат >5 мин без действий не показывает ложное «Соединение закрыто» из-за timeout (или reconnect восстанавливает без потери сессии — см. UI-план).
- [ ] Реальные ошибки сервера по-прежнему видны как error, не только closed.
- [ ] `ruff check .` и `pytest` зелёные.
