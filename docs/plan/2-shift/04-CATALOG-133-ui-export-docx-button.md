# CATALOG-133 — ui: кнопка «Выгрузить в docx» в карточке прогона

- **Задача Plane:** [CATALOG-133](https://app.plane.so/belchch/projects/catalog-app/work-items/133) (id: `02c1e43a-952e-42a9-841d-02bbb5a4d65c`, state: In Progress)
- **Статус плана:** Analyzed
- **Тип шага:** ui
- **Очередь:** 04 · blocked_by CATALOG-132
- **Цель:** В карточке прогона и у открытого документа кнопка «Выгрузить в docx» с состояниями idle / загрузка / готово (путь + копировать) / ошибка.

## Постановка задачи (актуальное ТЗ)
_(источник: описание задачи — комментариев не было)_

- Кнопка в карточке результата прогона (`RunView`) и для открытого документа.
- Состояния: «выгружаю» (disabled + индикатор) / «готово» с относительным путём `export/...` / ошибка с `detail` из API.
- Путь с кнопкой «скопировать» — файл уже в папке пользователя, скачивание не нужно.

Вне скоупа: `FileResponse`, «показать в Finder», мультиселект в `DocumentList`.

## Предыстория
> Это уже было: старое описание и предыдущие обсуждения. Приведено для контекста, НЕ является актуальным заданием.

_нет — комментариев к задаче не было_

## Контекст
Предусловие: `docs/plan/2-shift/03-CATALOG-132-code-export-docx-api.md` (`POST /export/docx`).

- `frontend/src/components/RunView.tsx` — карточка прогона, кнопки экспорта нет.
- `frontend/src/api.ts` — клиент API без `export/docx`.
- `frontend/src/components/DocumentList.tsx` — `onSelect: (id: string) => void`, однозначный выбор; мультиселект вне скоупа.

## Затрагиваемые файлы
- `frontend/src/api.ts` — `exportDocx({ doc_ids, title?, template? })`.
- `frontend/src/components/RunView.tsx` — кнопка и три/четыре состояния.
- Компонент открытого документа (viewer/панель) — та же кнопка на текущем `doc_id`.
- Тест компонента на idle / loading / error / путь.

## План действий
1. Клиент `POST /export/docx`; ошибки — `detail` с бэкенда.
2. Кнопка в `RunView` (doc_ids результата/входа прогона) и у открытого документа.
3. Состояния: idle → loading (disabled + индикатор) → success (`export/...` + copy) или error.
4. Дизайн-спека; токены `docs/ui-style-guide.md`.
5. Тест трёх состояний и отображения пути.

## Критерии приёмки (Definition of Done)
- [ ] Кнопка есть в карточке прогона и у открытого документа.
- [ ] Три состояния + путь с копированием.
- [ ] Дизайн-спека и визуальные критерии выполнены.
- [ ] `pnpm run build`, `lint`, `typecheck`, `test` из `frontend/`.
