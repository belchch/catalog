# CATALOG-58 — Ошибка создания скилла и таймаут сессии

- **Задача Plane:** [CATALOG-58](https://app.plane.so/belchch/projects/catalog-app/work-items/58) (id: `17d4dccd-ac7e-4a47-9078-b2f510ff47aa`, state: In Progress)
- **Статус плана:** Analyzed
- **Тип шага:** ui
- **Цель:** После неудачного «Создать скилл» пользователь **всегда** видит полезную ошибку и выходит из load state; при таймауте — модалка предложить увеличить timeout сессии (default 60). Предусловие: `CATALOG-58-code-session-timeout-errors.md`.

## Постановка задачи (актуальное ТЗ)

_(источник: описание задачи)_

Кнопка внизу чата для создания скилла: висит в load state, ошибки не видно.

Дополнительно: модалка увеличить таймаут сессии — изначально 60; пользователь переопределяет в контексте сессии чата.

## Предыстория

> Это уже было: старое описание и предыдущие обсуждения. Приведено для контекста, НЕ является актуальным заданием.

_нет — комментариев к задаче не было_

## Контекст

- Кнопка: `Chat.tsx:265-275` → `App.handleCreateSkill` (`:176-188`).
- Сейчас ошибка → `setNotice` в header (`App.tsx` ~243) — легко не заметить; `buildingSkill` сбрасывается в `finally`, но при очень долгом запросе UX «висит».
- Chat `error` — только planner WS, не build.
- Модалки timeout / session timeout UI — нет.

## Затрагиваемые файлы

- `frontend/src/App.tsx` — обработка ошибок build, открытие модалки timeout
- `frontend/src/components/Chat.tsx` — явный error/banner у кнопки создания скилла
- `frontend/src/components/SessionTimeoutModal.tsx` — **новый** (или аналог)
- `frontend/src/api.ts` — `buildSkill` / patch session timeout; парсинг `detail`
- При необходимости: типы сессии в hooks

## План действий

1. **Сброс loading + видимая ошибка.** Гарантировать `buildingSkill=false` после fail; показать error рядом с кнопкой / красный banner (не только бледный notice).
2. **Текст ошибки.** Показывать `detail` с backend (таймаут, retries, «заполните артефакт» после CATALOG-53).
3. **Модалка timeout.** При timeout-ошибке (или по действию) — предложить увеличить значение; PATCH session; подтверждение.
4. **Отображение текущего timeout** в UI сессии (компактно) — чтобы пользователь видел override.
5. **Проверки.** lint/typecheck/build; ручной: симулировать fail build → ошибка видна, кнопка жива; увеличить timeout через модалку.

## Критерии приёмки (Definition of Done)

- [ ] После fail build кнопка не остаётся в «Собираю скилл…».
- [ ] Пользователь видит понятный текст ошибки без открытия DevTools.
- [ ] При таймауте доступна модалка увеличить timeout сессии (база 60).
- [ ] Новое значение timeout сохраняется для текущей сессии чата.
- [ ] `frontend/`: `pnpm run lint`, `typecheck`, `build` зелёные.
- [ ] Соответствие дизайн-спеке UI-шага.
