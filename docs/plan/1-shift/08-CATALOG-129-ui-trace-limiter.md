# CATALOG-129 — Трейс: глубина вложенности и сработавший ограничитель

- **Задача Plane:** [CATALOG-129](https://app.plane.so/belchch/projects/catalog-app/work-items/129) (id: `46eb322e-97f4-43e1-bdda-65d27735295b`, state: In Progress)
- **Статус плана:** Analyzed
- **Тип шага:** ui
- **Очередь:** 08 · blocked_by CATALOG-124 · blocked_by CATALOG-125 · blocked_by CATALOG-126 · blocked_by CATALOG-127
- **Цель:** В дереве трейса видны глубина вложенного запуска, цена и остаток бюджета. Срабатывание ограничителя — отдельный понятный узел, не «скилл просто не сработал».

## Постановка задачи (актуальное ТЗ)
_(источник: описание задачи — комментариев не было)_

Тип: ui. Последняя в очереди; нужны CATALOG-124…127.

CATALOG-119 уже сделал раскрываемые узлы вложенных запусков. Добавить данные:

1. `depth` в заголовке узла.
2. Фактическая цена запуска в LLM-вызовах и остаток бюджета хода.
3. Отдельный узел при ограничителе: бюджет вызовов / лимит запусков / дедлайн / глубина — человеческие формулировки, не код ошибки.
4. Пометка, если скилл не предложили модели из-за глубины или цикла.

## Предыстория
> Это уже было: старое описание и предыдущие обсуждения. Приведено для контекста, НЕ является актуальным заданием.

_нет — комментариев к задаче не было_

## Контекст
Предусловия: depth в трейсе (124), budget/deadline tool result и trace node (125/126), реальные agent/pipeline вызовы (127).

- `frontend/src/components/TraceSteps.tsx` — дерево по `parent_run_id` / `childRunId`.
- `frontend/src/lib/traceSegments.ts` — сборка шагов из трейса.
- `frontend/src/hooks/useRunStream.ts` — живой стрим.
- Отказ тула сегодня — обычный `tool_result` с `ok: false`; нужно различать `budget exhausted` / deadline / depth.

Если бэкенд ещё не отдаёт «скилл скрыт из реестра» как событие — минимально: узел/пометка из trace entry, который положат 124–126. Не выдумывать отдельный протокол сверх уже записанных полей.

Токены — `docs/ui-style-guide.md`.

## Затрагиваемые файлы
- `frontend/src/lib/traceSegments.ts` — `depth`, cost, budget remainder, limiter reason.
- `frontend/src/hooks/useRunStream.ts` — те же поля в живом стриме, если приходят в tool_result/trace.
- `frontend/src/components/TraceSteps.tsx` — заголовок узла + узел ограничителя.
- `frontend/src/components/TraceSteps.test.ts` / `TraceRunNode.test.tsx` — четыре причины и depth.

## План действий
1. Пробросить `depth`, cost, remainder из трейса/tool result в модель шага/узла.
2. В заголовке вложенного запуска показать глубину, цену, остаток.
3. При лимитере — отдельный узел с одной из четырёх формулировок.
4. Если в трейсе есть факт «не зарегистрирован из-за chain/depth» — приглушённая пометка.
5. Тесты на четыре причины и на отсутствие регрессии дерева CATALOG-119.

## Критерии приёмки (Definition of Done)
- [ ] В трейсе видна глубина каждого вложенного запуска.
- [ ] Видны цена запуска и остаток бюджета.
- [ ] Четыре причины отказа различимы и читаются как объяснение.
- [ ] Только токены/примитивы из `docs/ui-style-guide.md`.
- [ ] `pnpm run build`, `lint`, `typecheck`, `test` из `frontend/`.
