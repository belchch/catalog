# CATALOG-123 — ADR: agent и pipeline скиллы как тулы сессии с ограничителем

- **Задача Plane:** [CATALOG-123](https://app.plane.so/belchch/projects/catalog-app/work-items/123) (id: `41072dab-ba2f-42b9-b4ad-e9891c9db4a1`, state: In Progress)
- **Статус плана:** Analyzed
- **Тип шага:** code
- **Очередь:** 02 · blocking CATALOG-124
- **Цель:** Зафиксировать в ADR решения по глубине, цепочке, бюджету LLM-вызовов, дедлайну хода и деградации в tool result. Код не менять — только документация, на которую опираются CATALOG-124…129.

## Постановка задачи (актуальное ТЗ)
_(источник: описание задачи — комментариев не было)_

Тип: code (только документация). Первая в очереди цепочки skill-as-tool.

ADR-0019 п.2: тулом сессии может быть только `kind=script`. Решение — разрешить все три `kind` и ввести ограничитель.

Записать:

1. Глубина 2. На третьем уровне skill-тулы не регистрируются (не отклоняются при вызове).
2. Скилл из текущей ветки `chain` не регистрируется.
3. Единица бюджета — LLM-вызов, не токен. Гибрид токенов — после CATALOG-122.
4. Бюджет хода: 60 вложенных LLM-вызовов и 20 вложенных запусков. Цикл планировщика в бюджет не входит.
5. Резерв по худшему случаю с возвратом остатка.
6. Дедлайн хода: `max(600, llm_timeout_seconds × 15)`.
7. Деградация — отказ в tool result, не исключение.

Критерии: файл ADR со статусом Accepted и `Extends: ADR-0019`; в ADR-0019 пометка, что п.2 пересмотрен; `docs/adr/README.md` обновлён.

## Предыстория
> Это уже было: старое описание и предыдущие обсуждения. Приведено для контекста, НЕ является актуальным заданием.

_нет — комментариев к задаче не было_

## Контекст
Номер **0020 занят**: `docs/adr/0020-llm-judge-custom-checks.md` (LLM-судья). Тикет просит `0020-skill-tool-budget.md` — это коллизия. Новый файл: `docs/adr/0021-skill-tool-budget.md`. В шапке и индексе — 0021, в тексте сослаться, что тикет называл 0020.

Опора:

- `docs/adr/0019-skill-as-session-tool.md:27-32` — п.2 script-only, п.3 глубина 1.
- `docs/adr/README.md:27-28` — индекс 0019/0020.
- Потолок без ограничителя: `run_agent` без явного `max_iterations` в `backend/catalog/api/sessions.py` (дефолт 8 в `runner.py`).

Реализация — следующие планы, не этот шаг.

## Затрагиваемые файлы
- `docs/adr/0021-skill-tool-budget.md` — новый ADR (Accepted, Extends ADR-0019).
- `docs/adr/0019-skill-as-session-tool.md` — пометка, что п.2 пересмотрен в ADR-0021.
- `docs/adr/README.md` — строка индекса.

## План действий
1. Написать ADR-0021 по шаблону существующих: Context, Decision (7 пунктов), Consequences, Alternatives (вызовы скиллов vs LLM; глубина 1; «продолжить за бюджет»; глубина как единственный лимит).
2. В ADR-0019 у п.2 добавить, что `agent`/`pipeline` разрешены ADR-0021 при ограничителе.
3. Добавить строку в индекс README.

## Критерии приёмки (Definition of Done)
- [ ] Есть `docs/adr/0021-skill-tool-budget.md`, Status Accepted, Extends ADR-0019.
- [ ] ADR-0019 явно говорит, что п.2 пересмотрен.
- [ ] `docs/adr/README.md` содержит 0021.
- [ ] Существующий ADR-0020 (LLM-судья) не переименован и не перезаписан.
- [ ] Код приложения не меняется.
