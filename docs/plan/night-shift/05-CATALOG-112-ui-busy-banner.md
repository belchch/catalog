# CATALOG-112 — UI: баннер busy «Агент отвечает»

- **Задача Plane:** [CATALOG-112](https://app.plane.so/belchch/projects/catalog-app/work-items/112) (id: `442b7390-d66e-429a-a749-a3e1eb139d67`, state: In Progress)
- **Статус плана:** Analyzed
- **Тип шага:** ui
- **Очередь:** 05 · предусловие: 04 (code того же тикета)
- **Цель:** Текст баннера смены воркспейса при `reason=session` — «Агент отвечает», не «Активна сессия чата».

## Постановка задачи (актуальное ТЗ)
_(источник: описание задачи — комментариев не было; UI-часть)_

Баннер в `WorkspacePicker.tsx` («Активна сессия чата») уточнить на «Агент отвечает». Семантика busy меняется в code-плане: блокировка только на время turn.

## Предыстория
_нет — комментариев к задаче не было_

## Контекст
Предусловие: `docs/plan/night-shift/04-CATALOG-112-code-busy-by-turn.md`.

- `frontend/src/components/WorkspacePicker.tsx:60-65` — `reason === 'session'`: title «Активна сессия чата — переключение воркспейса недоступно», short «Активна сессия чата».
- Тот же файл, `:51-52` — общий hint про завершение; для session его можно оставить или сузить до «после ответа агента».
- Проверить тесты/копии, если есть поиск по этой строке.

## Затрагиваемые файлы
- `frontend/src/components/WorkspacePicker.tsx` — title/short (и hint, если врёт).
- Тесты пикера, если завязаны на старый текст.

## План действий
1. Заменить формулировки session-busy на «Агент отвечает».
2. Hint не должен обещать «закрой чат» — чат можно оставить открытым.
3. `pnpm run build`, `lint`, `typecheck`, `test`.

## Критерии приёмки (Definition of Done)
- [ ] При `reason=session` пользователь видит «Агент отвечает», не «Активна сессия чата».
- [ ] Hint не требует закрыть чат.
- [ ] `reason=run` не менять.
- [ ] Frontend-проверки зелёные.
