# CATALOG-93 — Фильтр по имени в списке папок пикера воркспейса (дубликат CATALOG-92)

- **Задача Plane:** [CATALOG-93](https://app.plane.so/belchch/projects/catalog-app/work-items/93) (id: `81437995-66c7-497a-8c48-ca597798134a`, state: In Progress)
- **Статус плана:** Analyzed
- **Тип шага:** ui
- **Очередь:** 08 · blocked_by CATALOG-91
- **Цель:** Тот же фильтр по имени в списке папок `WorkspacePicker`, что и в [CATALOG-92](docs/plan/night-shift/07-CATALOG-92-ui-picker-folder-filter.md). Тикет — полный дубликат по тексту ТЗ; работа выполняется один раз, второй тикет закрывается тем же PR.

## Постановка задачи (актуальное ТЗ)
_(источник: описание задачи; комментариев нет. Текст побайтово совпадает с CATALOG-92)_

**Фильтр по имени в списке папок пикера воркспейса**

Контекст: WorkspacePicker (`frontend/src/components/WorkspacePicker.tsx`) показывает плоский список подпапок текущего уровня, полученный из `GET /fs/browse`. В папках с несколькими десятками подпапок нужную приходится искать глазами. ADR-0017 фиксирует отсутствие поиска как известный минус серверного пикера.

Скоуп: только frontend. Бэкенд и API не меняются — фильтруем уже загруженный массив `entries` на клиенте. Рекурсивный поиск по дереву в эту задачу не входит.

Что сделать:

1. Над списком папок (секция «Обзор папок») добавить поле ввода с плейсхолдером «Фильтр по имени» и доступным label.
2. Список фильтруется по подстроке в `entry.name`, без учёта регистра и с обрезкой пробелов по краям. Совпадения в пути не учитываем — только имя папки.
3. Значение фильтра сбрасывается при переходе внутрь папки (`goInto`) и вверх (`goUp`), то есть каждый новый уровень открывается с пустым фильтром.
4. Если после фильтрации ничего не осталось — показать «Ничего не найдено» вместо существующего «Нет вложенных папок» (это разные состояния).
5. Поле скрывается или не показывается, когда на уровне нет папок вообще, а также во время загрузки и при ошибке обзора.
6. Секция «Недавние» фильтром не затрагивается.

Критерии приёмки перенесены в раздел ниже.

## Предыстория
_нет — комментариев к задаче не было._

## Контекст
- **Это дубликат.** [CATALOG-92](docs/plan/night-shift/07-CATALOG-92-ui-picker-folder-filter.md) (id `6ea7f82c-22b7-48f6-9f45-8103b90bddf1`) содержит идентичный текст ТЗ; тикеты созданы почти одновременно. Полный анализ кода, точки изменения и решения по реализации — в плане CATALOG-92, здесь они не дублируются.
- Практический вывод: реализовывать фильтр нужно **один раз**. Этот шаг pipeline должен либо не давать дифа (если CATALOG-92 уже выполнен), либо выполнить работу и закрыть оба тикета. Решение, какой тикет считать основным, а какой закрыть как duplicate в Plane, — за человеком.
- Ключевые точки изменения (для сверки, подробности — в плане CATALOG-92): состояние фильтра рядом с `entries` (`frontend/src/components/WorkspacePicker.tsx:55-61`), поле ввода после строки навигации (`:220-243`), ветвление состояний списка (`:245-282`), фильтрация перед `map` (`:253`), сброс при смене уровня (`goInto` `:144-150`, `goUp` `:152-156`).
- Пересечение: [CATALOG-91](docs/plan/night-shift/06-CATALOG-91-ui-picker-result-panel-loader.md) правит ту же модалку — конфликт правок вероятен, выполнять последовательно.

## Затрагиваемые файлы
- `frontend/src/components/WorkspacePicker.tsx` — те же изменения, что в плане CATALOG-92. Если фильтр уже реализован в рамках CATALOG-92, файлы не меняются.

## План действий
1. Проверить текущее состояние `frontend/src/components/WorkspacePicker.tsx`: есть ли уже поле фильтра и ветка «Ничего не найдено».
2. Если фильтр уже реализован (CATALOG-92 выполнен) — новых правок не вносить, зафиксировать в отчёте шага, что работа покрыта предыдущим тикетом, и оставить тикет CATALOG-93 на закрытие как duplicate.
3. Если фильтра нет — выполнить план из [CATALOG-92](docs/plan/night-shift/07-CATALOG-92-ui-picker-folder-filter.md) целиком (шаги 1–11) и указать в PR оба тикета.
4. В обоих случаях прогнать из `frontend/`: `pnpm run build`, `pnpm run lint`, `pnpm run typecheck`.

## Критерии приёмки (Definition of Done)
- [ ] Фильтр по имени в списке папок работает согласно критериям плана CATALOG-92 (мгновенное сужение без запросов, сброс при смене уровня, разные сообщения для пустого результата и пустой папки, «Недавние» не затронуты, доступность не сломана).
- [ ] Функциональность реализована один раз: в репозитории нет двух независимых реализаций фильтра.
- [ ] В PR или отчёте шага явно указано, что CATALOG-93 — дубликат CATALOG-92, и какой тикет закрывается как duplicate.
- [ ] Бэкенд и API не изменены.
- [ ] Из `frontend/`: `pnpm run build`, `pnpm run lint`, `pnpm run typecheck` — зелёные.
