# CATALOG-29 — Многострочный чат

- **Задача Plane:** [CATALOG-29](https://app.plane.so/belchch/projects/catalog-app/work-items/29) (id: `629221cb-d8e5-4319-9a85-02e6aa820173`, state: In Progress)
- **Статус плана:** Analyzed
- **Тип шага:** ui
- **Цель:** Многострочный ввод в чате: `Shift+Enter` — новая строка, `Enter` — отправка.

## Постановка задачи (актуальное ТЗ)
_(источник: описание задачи)_

В чате добавить многострочность. Переход на новую строку по Shift+Enter.

## Предыстория

_нет — комментариев к задаче не было_

## Контекст

- Сейчас однострочный `<input>`, любой Enter → submit — `Chat.tsx:110-117`.

## Затрагиваемые файлы

- `frontend/src/components/Chat.tsx` — заменить на `<textarea>`, обработчики клавиш, базовый auto-resize по желанию.

## План действий

1. `<textarea>` вместо `<input>`.
2. `Enter` без Shift → submit; `Shift+Enter` → перевод строки.
3. Сохранить кнопку «Отправить».
4. Ручная проверка многострочного сообщения в ленте.

## Критерии приёмки (Definition of Done)

- [ ] Shift+Enter добавляет строку; Enter отправляет.
- [ ] Многострочный текст корректно уходит в planner.
- [ ] `pnpm run build/lint/typecheck` зелёные.
